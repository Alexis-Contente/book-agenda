generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./book-agenda.db"
}

model contact {
  id          Int     @id @default(autoincrement())
  name        String?
  firstname   String?
  email       String?
  information String?
  birth       String?
}
